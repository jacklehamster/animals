import { setShowSplashScreen, setEnablePhysicsSolver, setGamepadsEnable, setCanvasFixedSize, vec2, setCanvasPixelated } from "littlejsengine";

setShowSplashScreen(true);
setEnablePhysicsSolver(true);
setGamepadsEnable(false);

setCanvasFixedSize(vec2(1600, 1200));
setCanvasPixelated(false);

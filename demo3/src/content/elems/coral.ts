import type { Elem } from "../../definition/elem";
import { SIZE } from "../constant";

export const CORAL: Elem = {
  definition: "coral",
  group: {
    grid: [SIZE + 1, SIZE + 1],
    chance: 0.1,
  },
}
